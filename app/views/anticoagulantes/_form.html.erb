<%= form_for(@anticoagulante) do |f| %>
  <% if @anticoagulante.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@anticoagulante.errors.count, "error") %> prohibited this anticoagulante from being saved:</h2>

      <ul>
      <% @anticoagulante.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :descripcion %><br>
    <%= f.text_area :descripcion %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
